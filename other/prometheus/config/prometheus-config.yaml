global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'chat-service-prometheus'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 2s
    static_configs:
      - targets: ['chat-service:8080']
        labels:
          app: 'chat-service-prometheus'